# beaterOS ownership.
#
# Per the multi-agent governance model (docs/governance/review-protocol.md),
# code is NOT siloed to its author: every reviewer has authority over all of it.
# GitHub CODEOWNERS operates on accounts, and every agent shares the @jadenfix
# account, so this file assigns broad ownership to the repo owner rather than
# splitting the tree. The real author != reviewer != merger rule is enforced at
# the agent-identity layer via the coordination ledger and
# scripts/check-governance.py.

# Everything is owned (and reviewable/mergeable) by the repo owner.
*                               @jadenfix

# The governance process itself — changes here affect every agent, so they get
# extra scrutiny in review even though the owner is the same account.
/docs/governance/               @jadenfix
/CONTRIBUTING.md                @jadenfix
/scripts/check-governance.py    @jadenfix
